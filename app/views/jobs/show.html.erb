
  <div class="container-fluid row" id="show">
    <div class="col-md-6 col-md-offset-2" >
  <div class="p1"><%= simple_format(@job.company) %></div>
  <h1> <%= @job.title %><span><%= @job.created_at %></span></h1>
  <div class="p2"><%= @job.wage_lower_bound %>-<%= @job.wage_upper_bound %></div>

</div>
<div class="col-md-2 col-md-offset-2 vote position-deal clearfix">

  <%= link_to("投交履历", new_job_resume_path(@job)) %>
</div>
</div>
<div class="col-md-6 col-md-offset-2">
<p><%= simple_format(@job.description) %></p>

contact_email:<p><%= simple_format(@job.contact_email) %></p>

</div>
